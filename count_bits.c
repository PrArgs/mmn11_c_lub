#include <stdio.h>
#include <stdlib.h>

#define count_bits;

/* 
count_bits: 
return nuber of on bits in an even locacation in an unsigned long 
 */

int count_bits(unsigned long value)
{
    int count = 0;

    for(i=long)
    {       
    return count;
    }
}