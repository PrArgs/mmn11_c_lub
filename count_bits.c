#include <stdio.h>
#include <stdlib.h>

int count_bits(unsigned long value);

/* 
count_bits: 
return number of on bits in an even location in an unsigned long
 */

int count_bits(unsigned long value)
{
    int count = 0;
    return 0;
}
